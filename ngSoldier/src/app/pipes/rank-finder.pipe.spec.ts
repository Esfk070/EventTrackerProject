import { RankFinderPipe } from './rank-finder.pipe';

describe('RankFinderPipe', () => {
  it('create an instance', () => {
    const pipe = new RankFinderPipe();
    expect(pipe).toBeTruthy();
  });
});
